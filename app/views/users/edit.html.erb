<div class = "container">
  <div class="row no-gutters">
    <div class="mx-sm-auto shadow-lg rounded px-0 my-5">
      <h5 class="text-white bg-primary p-4 mb-0">プロフィール編集</h5>
      <div class="form-body p-4">
      <!--上記クラスで投稿フォームの枠を作成-->
      <% if @user.errors.any? %>
        <ul class="alert alert-danger" role="alert">
          <h6 class="alert-heading">
            <%= @user.errors.count %>件のエラーが発生しました
          </h6>
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
      <!--↑エラーメッセージを表示する為の記述。モデルへのバリデーションの記載要。コントローラにif文での遷移先指定-->
  　   <%= form_with model: @user do |f| %>
        <div class="input-group mb-3 mt-4">
            <div class="input-group-prepend">
              <span class="input-group-text" aria-hidden="true"><i class="fas fa-user"></i></span>
            </div>
            <%= f.text_field :name, class:"form-control", placeholder:"名前" %>
          </div>

        <div class="input-group mb-3 mt-4">
          <%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*", class: "w-100" %>
        </div>
        <hr>

        <%= f.submit "変更を保存", class: "btn btn-lg btn-block btn-primary mt-4" %>
      <% end %>
      </div>
  </div>
</div>